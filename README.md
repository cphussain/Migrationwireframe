
  # VMware to Openshift Migration Wireframe

  This is a code bundle for VMware to Openshift Migration Wireframe. The original project is available at https://www.figma.com/design/NGRqHimsLdtEeSwCtanMCt/VMware-to-Openshift-Migration-Wireframe.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  